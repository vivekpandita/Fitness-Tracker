1. Open Terminal in VS code and go till client folder using cd command. like cd client
2. Then run this command to check if node is already installed --> node -v
3. If node already present the run --> npm install --legacy-peer-deps
4. Then start application --> npm start